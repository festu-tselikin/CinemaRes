@model IEnumerable<Cinema.Models.ECRB>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ECRBName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ECRBName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ECRBId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ECRBId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ECRBId })
        </td>
    </tr>
}

</table>
